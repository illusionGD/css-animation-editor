/**
 * 画布相关常量
 * 这些常量在多个模块中使用（Canvas、Project、Export 等），需要保证一致性
 */

/** 画布默认宽度（像素） */
export const CANVAS_DEFAULT_WIDTH = 1920

/** 画布默认高度（像素） */
export const CANVAS_DEFAULT_HEIGHT = 1080

/** 画布默认背景颜色 */
export const CANVAS_DEFAULT_BACKGROUND_COLOR = '#ffffff'

/** 画布默认网格大小（像素） */
export const CANVAS_DEFAULT_GRID_SIZE = 20

/** 画布默认缩放级别（1 = 100%） */
export const CANVAS_DEFAULT_ZOOM = 1

/** 画布默认 X 轴偏移（像素）- 为垂直标尺留出空间，使画布(0,0)对齐标尺(0,0) */
export const CANVAS_DEFAULT_OFFSET_X = 20

/** 画布默认 Y 轴偏移（像素）- 为水平标尺留出空间，使画布(0,0)对齐标尺(0,0) */
export const CANVAS_DEFAULT_OFFSET_Y = 20

/** 画布默认显示网格 */
export const CANVAS_DEFAULT_SHOW_GRID = true

/** 画布默认显示标尺 */
export const CANVAS_DEFAULT_SHOW_RULER = true
